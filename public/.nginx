# nginx configuration

index index.php;

location ~ 403 {
  rewrite ^(.*)$ ^/(application|extensions|include|\.git|\.svn|\.hg).*$ redirect;
}

location / {
  if (!-e $request_filename){
    rewrite ^(.*)$ /index.php?$1 break;
  }
}