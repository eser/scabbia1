(function( $, undefined ) {

	$.browser = {
		opera: true,
		version: '12.02'
	};

	/*
	$.getJSON = function(path, values, fnc) {
		if(typeof values == 'function') {
			return $l.ajax.get(path, null, values);
		}

		return $l.ajax.get(path, values, fnc);
	};
	*/

})(jQuery);